// import { level, randomId, score, startGame } from './script';

export function restartGame() {
  location.reload();

  // const stage = document.querySelectorAll('.stage');
  // const birdItem = document.querySelectorAll('.bird-item');
  // const scoreCount = document.querySelector('.score-count');
  // const audio = document.querySelector('audio');
  // const next = document.querySelector('.next');
  // const birdDescription = document.querySelector('.bird-description');
  // const sound = document.querySelector('.audio-player');
  // const restart = document.querySelector('.restart');
  // const birdName = document.querySelector('.bird-name');

  // randomId = 0;
  // level = 0;
  // score = 0;

  // scoreCount.textContent = score;

  // stage.forEach((item) => item.remove());
  // birdItem.forEach((item) => item.remove());
  // audio.remove();
  // next.textContent = `next ${level + 1}/6`;
  // birdDescription.style.display = 'block';
  // sound.style.display = 'flex';
  // next.disabled = true;
  // birdName.innerHTML = `**** <br> Угадай исполнителя`;
  // restart.remove();

  // startGame();
}
